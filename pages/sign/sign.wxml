<!--pages/sign/sign.wxml-->
<van-calendar
  show-title="{{ false }}"
  poppable="{{ false }}"
  min-date="{{ minDate }}"
  max-date="{{ maxDate }}"
  show-confirm="{{ false }}"
  formatter="{{ formatter }}"
/>
<view class="btn">
  <button bindtap="sign">签到</button>
</view>
